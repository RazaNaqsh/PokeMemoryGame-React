import { nanoid } from "nanoid";
import React, { useEffect, useState } from "react";

const PokeCards = () => {
	const [pokemons, setPokemons] = useState([]);

	return (
		<div>
			<h1 className="text-center">PokeCard</h1>
			<div></div>
		</div>
	);
};

export default PokeCards;
